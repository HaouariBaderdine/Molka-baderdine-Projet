import { ArticleFilter1Pipe } from './article-filter1.pipe';

describe('ArticleFilter1Pipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleFilter1Pipe();
    expect(pipe).toBeTruthy();
  });
});
